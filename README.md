指定文件夹容量监控工具

当指定的文件夹容量超过指定限制后，会删除较旧的文件。

软件首次开机会生成一个配置文件，默认会监视D:\ftp目录，容易限制为10GB

The specified folder capacity monitoring tool 

deletes older files when the specified folder capacity exceeds the specified limit.
